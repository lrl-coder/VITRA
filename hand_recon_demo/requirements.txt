# 3D手部重建Demo依赖列表

# 核心依赖
numpy>=1.20.0
torch>=1.10.0
opencv-python>=4.5.0
matplotlib>=3.3.0
tqdm>=4.60.0

# 图像处理
Pillow>=8.0.0
imageio>=2.9.0

# 科学计算
scipy>=1.7.0

# 命令行接口
argparse

# 可选：更好的3D渲染（推荐）
# trimesh>=3.9.0
# pyrender>=0.1.45

# 注意：
# 1. 本项目依赖于VITRA项目的以下模块：
#    - data/tools/utils_hawor.py (HaWoR流水线)
#    - data/tools/utils_moge.py (MoGe流水线，本demo不使用)
#    - libs/models/mano_wrapper.py (MANO模型包装器)
#
# 2. 需要下载以下模型权重文件：
#    - HaWoR: ./weights/hawor/checkpoints/hawor.ckpt
#    - 检测器: ./weights/hawor/external/detector.pt
#    - MANO: ./weights/mano/
#
# 3. CUDA支持（推荐）：
#    如果使用GPU加速，请确保安装了CUDA和对应版本的PyTorch
#    访问 https://pytorch.org/ 获取安装说明
